/*jshint -W004 */
var $db = require(global.cwd + '/lib/mongo');
var debug = require('debug')('wol:leaderboard');

exports.reserve = function reserve(game) {

};

/* create a new gameId */
function generate(game) {
  return new Promise(function(resolve, reject) {

  });
}